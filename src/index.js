import foo from './foo'
// import _ from 'lodash'
// console.log(_.defaults({ 'a': 1 }, { 'a': 3, 'b': 2 }));
// import npminfo from '../package.json'

console.log(foo)

const fn = () => {
	console.log(...[1, 2, 3]);
}
fn()